二叉树表示（链表，数组）
数组：一般数组（看做完全二叉树，下标标识
编号）
看做完全二叉树，将结点内容和编号关联起来，
储存在结构（左右儿子，本身内容）里面，相当
于是一个三元组，最后按照顺序构建结构数组


如何输入
输入结点内容（char），左儿子右儿子的编号
（在结构数组里面的下标，不一定是下上左右遍历
后的结果）
%c %d %d
根节点，它所在的下标没有被任何结点储存
时间复杂度O（n*n）
或者读取的时候同时设置非根节点
返回根节点的下标

建立两个二叉树

如何判断同构


一般树的表示方法
（低效率的，令每个结点的指向对象数
固定，存储浪费）
（大儿子兄弟表示法，两个指针，一个指向
第一个儿子，一个指向本人的兄弟）有二叉树
的味道了

二叉树，一个数据，一个左儿子，一个右儿子

二分查找

顺序存储，编号固定
链表存储，节省空间

